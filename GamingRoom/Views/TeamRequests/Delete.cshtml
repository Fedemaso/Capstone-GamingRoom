@model GamingRoom.Models.TeamRequest

@{
    ViewBag.Title = "Elimina Richiesta di Team";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container" style="min-height:100vh">
    <div class="row justify-content-center align-items-center" style="height:100%">
        <div class="col-md-8">
            <div class="card mt-5 text-white" style="background-color: rgb(157, 0, 255)!important">
                <div class="card-header">
                    <h2 class="text-center">Sei sicuro di voler eliminare questa richiesta di team?</h2>
                </div>
                <div class="card-body">
                    <dl class="dl-horizontal">
                        <dt>Nome Team</dt>
                        <dd>@Html.DisplayFor(model => model.Name)</dd>
                        <dt>Descrizione</dt>
                        <dd>@Html.DisplayFor(model => model.Description)</dd>
                        <dt>Foto Proposta</dt>
                        <dd>@Html.DisplayFor(model => model.ProposedPhoto)</dd>
                    </dl>
                </div>
                <div class="card-footer text-center">
                    @using (Html.BeginForm("Delete", "TeamRequests", new { id = Model.TeamRequestId }, FormMethod.Post))
                    {
                        @Html.AntiForgeryToken()
                        <button type="submit" class="btn btn-danger">Elimina</button>
                    }
                    <a href="@Url.Action("Index")" class="btn btn-secondary">Annulla</a>
                </div>
            </div>
        </div>
    </div>
</div>
