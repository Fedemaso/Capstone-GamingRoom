@model IEnumerable<GamingRoom.Models.EventTitles>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table text-light fs-5">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Events.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Titles.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Events.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Titles.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EventTitleID }) |
            @Html.ActionLink("Details", "Details", new { id=item.EventTitleID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EventTitleID })
        </td>
    </tr>
}

</table>
